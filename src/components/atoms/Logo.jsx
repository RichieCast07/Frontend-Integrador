import React from 'react';
import logo from '../../assets/Logo.png';

const Logo = () => {
    return (
        <img src={logo} alt="JESSY Logo" className="header-line" />
    );
};

export default Logo;
